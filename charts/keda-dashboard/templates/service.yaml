apiVersion: v1
kind: Service
metadata:
  name: {{ include "keda-dashboard.fullname" . }}
  labels:
    {{- include "keda-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keda-dashboard.selectorLabels" . | nindent 4 }}
